[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every route returns index.html so client-side routing works
# on direct visits and page refreshes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
